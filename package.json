{
  "name": "qicai-yunnan-tiemai-rebuild",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:privacy": "node scripts/check-privacy.mjs",
    "check:content": "node scripts/check-content.mjs",
    "check:links": "node scripts/check-links.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run test && npm run build && npm run check:privacy && npm run check:content && npm run check:links"
  },
  "dependencies": {
    "astro": "^7.2.1"
  }
}
